class DataTypes:
    from builtins import print

    class DataTypes:

        x, c, x, sd = 10, 22, 54, 54
        print(x, c, x, sd)

    i = 'vinay dewar '

    print(len(i))
    v = [10, 20, 30, 25, 255]
    b = bytes(v)

    for x in b: print(x)

    b = [10, 22, 32, 5, 5]
    k = bytearray(b)
    b[0] = 99
    for b in k: print(b)
